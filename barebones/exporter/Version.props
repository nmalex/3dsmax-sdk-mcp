<?xml version="1.0" encoding="utf-8"?>
<!-- The version for this cartridge. The native payload project reads it to stamp its binary; keep it
     in step with cartridge.json and the row in ../README.md. -->
<Project>
  <PropertyGroup Label="Version">
    <CartridgeVersion>0.1.0</CartridgeVersion>
    <CartridgeVersionComma>0,1,0,0</CartridgeVersionComma>
  </PropertyGroup>
</Project>
